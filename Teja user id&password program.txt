// Online C compiler to run C program online
#include <stdio.h>
void main()
{
    int uid=5371;
    int upasd=9647;
    int id;
    int pasd;
    printf("enter the user id\n");
    scanf("%d",&id);
    printf("enter the password\n");
    scanf("%d",&pasd);
    if(uid==id)
    {
        if(upasd==pasd)
        {
            printf("log in successful");
        }
        else
        {
            printf("incorrect password");
        }
    }
    else
    {
        printf("incorret user id\n");
    }
}